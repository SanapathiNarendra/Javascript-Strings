function sumCalculator(){
    var startNumber = Number( document.getElementById("start").value);
     var endNumber = Number(document.getElementById("end").value);

   var sum = 0;
   for (let i=startNumber; i<=endNumber; i++){
    sum = sum + i
    
    
   }
}